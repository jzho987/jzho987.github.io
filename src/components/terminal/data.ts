export const files = new Map<string, string>([
    ["README.md", `
        # Web Shell \n
        
        Web shell is a mock shell.
        It can perform certain shell-like commands,
        but it is not a shell emulator.
        to get the list of commands web shell can perform,
        use wsh --help.
    `],
]);